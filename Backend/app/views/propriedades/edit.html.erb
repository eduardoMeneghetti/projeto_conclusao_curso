<% content_for :title, "Editing propriedade" %>

<h1>Editing propriedade</h1>

<%= render "form", propriedade: @propriedade %>

<br>

<div>
  <%= link_to "Show this propriedade", @propriedade %> |
  <%= link_to "Back to propriedades", propriedades_path %>
</div>
